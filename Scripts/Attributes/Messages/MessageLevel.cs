namespace InitialPrefabs.Attributes.Messages {

    /// <summary>
    /// A message level mapping from a non editor workspace to the Editor level.
    /// </summary>
    public enum MessageLevel : int {
        None    = 0,
        Info    = 1,
        Warning = 2,
        Error   = 3
    }
}
